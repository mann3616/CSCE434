// This is a debatable test: is the 'fi' the inner or outter if?
// From the text, it's for the outter
main
{
    if( 1 + 1 == 2) then
        call println();
    else if (10 >= 100^1) then
        call println();
    fi;

    call printInt(100^2);
    call println();
}.
